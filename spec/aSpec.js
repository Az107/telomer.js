var telomer = require("../build/index");
var t = new telomer.telomer();

var data = [1,2,3,4,5];

describe("telomer generation", function() {
    
    it("contains spec with an expectation", function() {
      expect(true).toBe(true);
    });
  });
